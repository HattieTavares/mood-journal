<%- include('partials/header.ejs') %>


<section>
    <!-- Greet user -->
    <span>Hello, <%= user.userName %>!</span>

<!-- link to Check In view -->
    <button><a href="/checkin">checkin</a></button>
</section>

<section class="container">
<!-- grid to display mood colors -->
    <!-- show current week -->
    <ul class="flex grid grid-cols-7 gap-0">
        <!-- Remove numerical placeholders once color coding code is tested against DB -->
        <li class="h-8 w-1/7 outline outline-moodBlue" id="day1">1</li>
        <li class="h-8 w-1/7 outline outline-moodBlue" id="day2">2</li>
        <li class="h-8 w-1/7 outline outline-moodBlue" id="day3">3</li>
        <li class="h-8 w-1/7 outline outline-moodBlue" id="day4">4</li>
        <li class="h-72 w-1/7 outline outline-moodBlue" id="day5">5</li>
        <li class="h-8 w-1/7 outline outline-moodBlue" id="day6">1</li>
        <li class="h-8 w-1/7 outline outline-moodBlue" id="day7">4</li>
    </ul>
    <br>
    <!-- mood history boxes -->
    <ul class="flex grid grid-cols-7 gap-0">
        <% for(var i=0; i<70; i++) { %>
            <!-- Remove period placeholder when height class starts working correctly -->
            <li class="h-100 min-h-full w-1/7 outline outline-1 outline-moodBlue">.</li>
        <% } %>
    </ul>
    <!-- show mood history -->
    <% if (checkin.length > 6) { %>
        <!-- add moods to bottom of screen -->
        
    <% } else { %>
        <p>Check in to get started.</p>
    <% } %>
</section>

<section>
<!-- logout button -->
<button><a href="/logout">logout</a></button>
</section>

<%- include('partials/footer.ejs') %>